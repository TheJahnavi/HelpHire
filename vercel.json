{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "outputDirectory": "dist/public",
        "distDir": "dist/public"
      }
    },
    {
      "src": "server/api-handler.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "{shared/**,server/**,package.json}"
      }
    },
    {
      "src": "server/test-env.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "{shared/**,server/**,package.json}"
      }
    },
    {
      "src": "server/vercel-entry.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "dist/**"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/api-handler.ts"
    },
    {
      "src": "/test-env",
      "dest": "/server/test-env.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/server/vercel-entry.ts"
    }
  ]
}